# R12.1 Re-audit (round 2)

## Item 2, cloned_clone_weak delay preservation
- Finding: PASS
- Evidence: `VOrder::from_init` still initializes clone shells with the default `1_000_000` us delay (`src/simulation.rs:359`, `src/simulation.rs:375`), and `VOrder::cloned_clone_weak` now constructs `let mut cloned = ...` then unconditionally assigns `cloned.delay = self.delay` after `VOrder::from_init` succeeds (`src/virtual_broker.rs:440`, `src/virtual_broker.rs:441`, `src/virtual_broker.rs:461`). This copies the stored `chrono::Duration` value, so it is value-agnostic and round-trips every source delay, not only the tested `5_000_000` us case. Call-site inventory from `src`/`tests`: async `orders()` snapshot (`src/async_virtual_broker.rs:146`, `src/async_virtual_broker.rs:151`), async `get()` result (`src/async_virtual_broker.rs:193`, `src/async_virtual_broker.rs:198`), async user-order mirror and place response data (`src/async_virtual_broker.rs:321`, `src/async_virtual_broker.rs:326`, `src/async_virtual_broker.rs:338`, `src/async_virtual_broker.rs:341`), async modify/cancel response data (`src/async_virtual_broker.rs:402`, `src/async_virtual_broker.rs:462`), sync user-order mirror and place response data (`src/virtual_broker.rs:249`, `src/virtual_broker.rs:255`, `src/virtual_broker.rs:269`, `src/virtual_broker.rs:272`), sync modify/cancel response data (`src/virtual_broker.rs:316`, `src/virtual_broker.rs:361`). No `examples/` or `benches/` paths exist in the repo file inventory.
- Risk notes (if any): None found. All call sites use `cloned_clone_weak` as an owned snapshot, response payload, or user-order mirror of canonical order state. None encode or assert the old reset-to-default delay behavior; preserving `self.delay` makes these surfaces more faithful to the stored order.

## Item 3, new/rewritten tests
- order_place_delay_kwarg_survives_orders_snapshot: PASS. The test places one default-delay order and one `delay = 5_000_000` order (`tests/parity_async_virtual.rs:501`, `tests/parity_async_virtual.rs:503`, `tests/parity_async_virtual.rs:509`, `tests/parity_async_virtual.rs:513`), then reads delays only through `b.orders().values()` (`tests/parity_async_virtual.rs:517`, `tests/parity_async_virtual.rs:518`, `tests/parity_async_virtual.rs:521`) and asserts default plus custom values (`tests/parity_async_virtual.rs:524`, `tests/parity_async_virtual.rs:525`). `AsyncVirtualBroker::orders()` builds that snapshot through `v.cloned_clone_weak()` (`src/async_virtual_broker.rs:146`, `src/async_virtual_broker.rs:151`), so the test exercises the clone path where round 1 found the bug, not a direct inner lookup. The sync source test uses the same `orders().values()` assertion shape (`tests/parity/test_virtual_broker.rs:446`, `tests/parity/test_virtual_broker.rs:461`, `tests/parity/test_virtual_broker.rs:467`, `tests/parity/test_virtual_broker.rs:468`).
- order_place_attaches_to_userid_when_registered: PASS. The async helper registers three users (`tests/parity_async_virtual.rs:57`, `tests/parity_async_virtual.rs:60`, `tests/parity_async_virtual.rs:61`, `tests/parity_async_virtual.rs:62`). The rewritten test performs two unattributed places, one place for each registered userid, and one unknown-user place (`tests/parity_async_virtual.rs:449`, `tests/parity_async_virtual.rs:455`, `tests/parity_async_virtual.rs:463`, `tests/parity_async_virtual.rs:465`, `tests/parity_async_virtual.rs:475`, `tests/parity_async_virtual.rs:479`). It asserts six total orders, `user_order_count(c) == Some(1)` for each registered user, unknown returns `None`, and both clients/users count are three (`tests/parity_async_virtual.rs:483`, `tests/parity_async_virtual.rs:485`, `tests/parity_async_virtual.rs:486`, `tests/parity_async_virtual.rs:489`, `tests/parity_async_virtual.rs:490`, `tests/parity_async_virtual.rs:491`). The new accessors read `users.len()` and per-user `orders.len()` directly (`src/async_virtual_broker.rs:166`, `src/async_virtual_broker.rs:167`, `src/async_virtual_broker.rs:175`, `src/async_virtual_broker.rs:180`, `src/async_virtual_broker.rs:181`). This matches sync scope: two unattributed places, one per registered client, one unknown userid, six total orders, and per-user `orders.len() == 1` (`tests/parity/test_virtual_broker.rs:377`, `tests/parity/test_virtual_broker.rs:379`, `tests/parity/test_virtual_broker.rs:384`, `tests/parity/test_virtual_broker.rs:389`, `tests/parity/test_virtual_broker.rs:390`, `tests/parity/test_virtual_broker.rs:398`, `tests/parity/test_virtual_broker.rs:404`, `tests/parity/test_virtual_broker.rs:405`, `tests/parity/test_virtual_broker.rs:406`).

## Verdict
R12.1 ACK, proceed to R12.2
