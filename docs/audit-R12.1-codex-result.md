1. Locking + concurrency — PASS: State is behind `parking_lot::Mutex<Inner>` (`src/async_virtual_broker.rs:41`, `src/async_virtual_broker.rs:70`); `place`/`modify`/`cancel` lock only after `clock.now()` (`src/async_virtual_broker.rs:204`, `src/async_virtual_broker.rs:220`, `src/async_virtual_broker.rs:334`, `src/async_virtual_broker.rs:349`, `src/async_virtual_broker.rs:393`, `src/async_virtual_broker.rs:408`), and the only awaits are in the trait adapter after inherent calls (`src/async_virtual_broker.rs:495`, `src/async_virtual_broker.rs:504`, `src/async_virtual_broker.rs:508`). RNG and `failure_rate` are read under the same lock (`src/async_virtual_broker.rs:226`, `src/async_virtual_broker.rs:227`; `src/async_virtual_broker.rs:350`, `src/async_virtual_broker.rs:351`; `src/async_virtual_broker.rs:409`, `src/async_virtual_broker.rs:410`). `tokio` remains dev-only (`Cargo.toml:33`, `Cargo.toml:40`).
2. Sync parity — FAIL: `place` sets the canonical order delay from kwarg/default (`src/async_virtual_broker.rs:249`, `src/async_virtual_broker.rs:252`, `src/async_virtual_broker.rs:294`), but public `orders()` snapshots use `cloned_clone_weak` (`src/async_virtual_broker.rs:146`, `src/async_virtual_broker.rs:151`); that clone omits `delay` (`src/virtual_broker.rs:431`, `src/virtual_broker.rs:450`) and `VOrder::from_init` resets delay to `1_000_000` (`src/simulation.rs:375`). This breaks the documented owned-clone accessor parity for custom-delay orders versus sync `test_virtual_broker_order_place_delay` (`tests/parity/test_virtual_broker.rs:446`).
3. Test coverage vs actual surface — CONCERN: The async harness does not port `test_virtual_broker_order_place_delay` (`tests/parity/test_virtual_broker.rs:446`, `tests/parity/test_virtual_broker.rs:468`), which would expose item 2 through `AsyncVirtualBroker::orders()` (`src/async_virtual_broker.rs:146`). The claimed userid attach coverage is also weak: `order_place_attaches_to_userid_when_registered` asserts only success and order count (`tests/parity_async_virtual.rs:441`, `tests/parity_async_virtual.rs:457`), not the per-user attachment asserted by sync (`tests/parity/test_virtual_broker.rs:405`, `tests/parity/test_virtual_broker.rs:406`).
4. Semver guard — PASS: Sync `VirtualBroker` signatures are still the existing sync surface (`src/virtual_broker.rs:163`, `src/virtual_broker.rs:281`, `src/virtual_broker.rs:325`). `src/lib.rs` adds only the async module/export lines for this phase (`src/lib.rs:4`, `src/lib.rs:20`) with no removed exports around the existing public uses (`src/lib.rs:19`, `src/lib.rs:21`).
5. Opportunistic persistence.rs fix — PASS: Plain-text `sqlite` accurately describes a feature-gated submodule (`src/persistence.rs:5`, `src/persistence.rs:7`) and matches the actual `#[cfg(feature = "persistence")] pub mod sqlite` declaration (`src/persistence.rs:26`, `src/persistence.rs:27`) without requiring the feature for rustdoc.

R12.1 NACK — fix items 2, 3 first
