# Excused-failure register for the parity gate (PORT-PLAN §4.1.2 + §14).
# Empty at R0; rows are added only after codex approval at the owning phase
# gate. A present-empty file deserializes cleanly to `excused_set = {}`.
